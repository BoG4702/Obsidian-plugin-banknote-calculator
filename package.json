{
  "name": "cash-savings",
  "version": "0.1.0",
  "description": "Obsidian plugin for cash counting and savings planning",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=main.js --format=cjs --platform=browser --target=es2018 --external:obsidian",
    "dev": "esbuild src/main.ts --bundle --outfile=main.js --format=cjs --platform=browser --target=es2018 --external:obsidian --sourcemap --watch"
  },
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.7.3"
  }
}
